using UnityEngine;

public class LocationPrinter : MonoBehaviour
{
  void Awake()
  {
    
  }

  // Start is called before the first frame update
  void Start()
  {
    Debug.Log("Dia chi cua mi la o: " + transform.position);
  }

  // Update is called once per frame
  void Update()
  {

  }
}
